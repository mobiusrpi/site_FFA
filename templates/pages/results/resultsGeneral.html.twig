{% extends 'base.html.twig' %}

{% block title %}          
    Classement
{% endblock %}

{% block body %}
  <style>
        @media print {
            .page-break {
                page-break-after: always;
            }
        }
        @media print {
            .no-print {
                display: none !important;
            }
            .print-only {
                display: block !important;
            }
        }   
        @media screen {
            .print-only {
                display: none;
            }
        }
        /* Règles générales */
        table.print-table {
            border-collapse: collapse;
            width: 100%;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        /* Bordures plus foncées / épaisses pour impression */
        @media print {
            table.print-table th,
            table.print-table td {
                border: 2px solid #000 !important;   /* 2px = plus épais */
                padding: 4px;                        /* ajuster l'espacement */
                background-color: white !important;  /* garantir fond blanc si besoin */
            }
            table.print-table thead th {
                background-color: silver !important; /* si tu veux en-tête gris */
            }
            /* cacher éléments non imprimables */
            .no-print { 
                display: none !important; 
            }
        }

        .sort-column {
            cursor: pointer;
            user-select: none;
        }
        .sort-column:hover {
            background-color:rgb(243, 158, 158);
        }
        .sort-arrow {
            color: #999 !important;
            text-decoration: none !important;
            pointer-events: auto;
        }

        .sort-arrow:hover {
            color: #000 !important;       
        }
    </style>
    <div class="container-fluid mt-4">
        <div class="row align-items-center mb-3 no-print">
            <div class="col-12 col-md-8 mx-auto">
                <h5 class="text-center">
                    <strong>Compétition {{ competition.name }}</strong></br>
                    {{ competition.startDate|format_datetime(locale='fr', pattern='dd MMMM') }}
                    au {{ competition.endDate|format_datetime(locale='fr', pattern='dd MMMM YYYY') }}
                </h5>
            </div>
            <div class="col-12 col-md-4 text-center text-md-end mt-2 mt-md-0">
                <button onclick="window.print()">🖨️ Imprimer</button>
                <a  href="javascript:history.back()" class="btn btn-outline-secondary btn-sm">
                    <i class="fa-solid fa-arrow-left me-1"></i> Retour
                </a>
            </div>
        </div>    
        <div class="row g-0"> 
            {% for category, crews in scoreByCategory %}
                <div class="row align-items-center mb-3 print-only">      
                    <div class="col-12 col-md-8 mx-auto">
                        <h5 class="text-center">
                            <strong>Compétition {{ competition.name }}</strong></br>
                                {{ competition.startDate|format_datetime(locale='fr', pattern='dd MMMM') }} au 
                                {{ competition.endDate|format_datetime(locale='fr', pattern='dd MMMM YYYY') }}  - 
                        </h5>
                    </div>
                </div> 
                <div class="col-12 col-lg-6 mt-4 page-break">
                    <h5 class="text-center">
                        <strong>
                            Catégorie
                            <a href="{{ path('test_results_per_category',{ id: competition.id, category: category }) }}"
                                class="text-primary text-decoration-underline fw-bold d-inline-flex align-items-center sort-column"
                                style="white-space: nowrap;"
                                title="Voir par categorie {{ category }}">
                                {{ category }}
                            </a>
                       </strong>
                    </h5>
                    <div class="table-responsive">
                        <table class="table table-bordered table-sm mt-4 print-table">
                            <thead>
                                <tr>
                                    <th>Class.</th>
                                    <th>
                                        {% if competition.typecompetition.id == 2 %}
                                            Pilote
                                        {% else %}
                                            Équipage
                                        {% endif %}
                                    </th>
                                        {% for testId, test in testNames %}
                                            {% set isNavOrAnrTest = test.label|slice(0, 3) in ['NAV', 'ANR'] %}
                                            <th class="text-center sort-column" data-key="{{ loop.index }}">
                                                {% if isNavOrAnrTest and test.hasDetail %}
                                                    {% if test.validated %}
                                                        <a href="{{ path('test_results_detail', { testId: testId }) }}"
                                                        class="text-primary text-decoration-underline fw-bold d-inline-flex align-items-center sort-column"
                                                        data-key="{{ loop.index }}" style="white-space: nowrap;"
                                                        title="Voir les détails de {{ test.label }}">
                                                            {{ test.label }}
                                                            <i class="fa-solid fa-sort sort-arrow ms-2" style="cursor:pointer;"></i>
                                                        </a>
                                                    {% else %}
                                                        <span class="fw-bold text-muted" style="white-space: nowrap;" title="Résultats non encore validés">
                                                            {{ test.label }}
                                                            <i class="fa-solid fa-sort sort-arrow ms-2" style="cursor:pointer;"></i>
                                                        </span>
                                                    {% endif %}
                                                {% else %}
                                                    <span class="fw-bold" style="white-space: nowrap;">
                                                        {{ test.label }}
                                                        <i class="fa-solid fa-sort sort-arrow ms-2" style="cursor:pointer;"></i>
                                                    </span>
                                                {% endif %}
                                            </th>
                                        {% endfor %}

                                    <th class="text-center sort-column" data-key="total">
                                        <span class="fw-bold" style="white-space: nowrap;">
                                            Total <i class="fa-solid fa-sort sort-arrow ms-2"></i>
                                        </span>
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for rank, crewData in crews %}
                                    <tr data-score="{{ crewData.total ?? -1 }}">
                                        <td class="text-center">{{ loop.index }}</td>

                                        {# Nom de l'équipage / pilote #}
                                        <td>
                                            {% if crewData.crew is iterable %}
                                                {{ crewData.crew.fullName ?? crewData.crew.name ?? '—' }}
                                            {% else %}
                                                {{ crewData.crew }}
                                            {% endif %}
                                        </td>

                                        {# Boucle sur les tests #}
                                        {% for testKey, displayName in testNames %}
                                            {% set result = crewData.tests[testKey] ?? {'nav': null, 'att': null, 'total': null, 'dns': false} %}
                                            {% set displayValue = result.total is same as(null) ? '_' : result.total %}
                                            {% set sortValue = result.total is same as(null) ? -1 : result.total %}

                                            {% if result.dns %}
                                                <td class="text-center" data-value="-1" data-key="{{ loop.index }}">{{ 'DNS' }}</td>
                                            {% else %}
                                                <td class="text-center" data-value="{{ sortValue }}" data-key="{{ loop.index }}">{{ displayValue }}</td>
                                            {% endif %}
                                        {% endfor %}

                                        {# Total général #}
                                        <td class="text-center" data-value="{{ crewData.total is same as(null) ? -1 : crewData.total }}" data-key="total">
                                            <strong>{{ crewData.total is same as(null) ? '_' : crewData.total }}</strong>
                                        </td>                                    
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            {% endfor %}
        </div>    
    </div>
    <script>
        document.querySelectorAll('.table').forEach(table => {
            const headers = table.querySelectorAll('.sort-column');

            headers.forEach((header) => {
                const icon = header.querySelector('.sort-arrow');
                let ascending = true;
                const key = header.dataset.key;

                if (icon && key) {
                    icon.addEventListener('click', (event) => {
                        event.preventDefault();
                        event.stopPropagation();

                        const tbody = table.querySelector('tbody');
                        const rows = Array.from(tbody.querySelectorAll('tr'));

                        rows.sort((a, b) => {
                            const aCell = a.querySelector(`td[data-key="${key}"]`);
                            const bCell = b.querySelector(`td[data-key="${key}"]`);

                            const aVal = parseFloat(aCell?.dataset.value ?? aCell?.textContent ?? -1);
                            const bVal = parseFloat(bCell?.dataset.value ?? bCell?.textContent ?? -1);

                            return ascending ? aVal - bVal : bVal - aVal;
                        });

                        ascending = !ascending;

                        rows.forEach(row => tbody.appendChild(row));

                        // Mise à jour des flèches
                        headers.forEach(h => {
                            const i = h.querySelector('.sort-arrow');
                            if (i) {
                                i.classList.remove('fa-sort-up', 'fa-sort-down');
                                i.classList.add('fa-sort');
                            }
                        });
                        icon.classList.remove('fa-sort');
                        icon.classList.add(ascending ? 'fa-sort-up' : 'fa-sort-down');

                        // Mise à jour du classement
                        if (key === 'total') {
                            let previousScore = null;
                            let rank = 0;
                            let displayRank = 0;
                            rows.forEach((row) => {
                                const score = parseFloat(row.getAttribute('data-score') ?? -1);
                                rank++;
                                if (score !== previousScore) displayRank = rank;
                                row.children[0].textContent = displayRank;
                                previousScore = score;
                            });
                        } else {
                            rows.forEach((row, i) => {
                                row.children[0].textContent = i + 1;
                            });
                        }
                    });
                }
            });
        });
    </script>


{% endblock %}